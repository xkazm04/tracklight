apiVersion: v1
kind: Service
metadata:
  name: {{ include "lighttrack.fullname" . }}
  labels:
    {{- include "lighttrack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "lighttrack.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
